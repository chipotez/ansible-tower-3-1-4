- name: Preparando /etc/hosts
  gather_facts: True
  hosts: nuevos
#  vars_prompt:
#     - name: "name"
#       prompt: "Ingresar nombre de host"
#       private: yes
  tasks: 
    - lineinfile:
        path: /etc/hosts
#        regexp: '^127\.0\.0\.1'
        line: '127.0.0.1 localhost'
        state: absent
#    - lineinfile:
#        dest=/etc/hosts
#        line='Hello, World!'
